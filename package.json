{
  "name": "standup-helper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "db:migrate:local": "wrangler d1 migrations apply DB --local --config wrangler.jsonc",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote --config wrangler.jsonc",
    "db:migrations:list": "wrangler d1 migrations list DB --remote --config wrangler.jsonc",
    "auth:schema": "auth generate --config better-auth.schema.ts --output .wrangler-auth-schema.sql --yes",
    "check": "tsc --noEmit && node --check public/app.js && node --check public/login.js && node --check public/dashboard.js && node --check public/room.js",
    "test": "node --experimental-strip-types --test"
  },
  "dependencies": {
    "better-auth": "1.7.5",
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "auth": "1.7.5",
    "typescript": "^7.0.2",
    "wrangler": "^4.34.0"
  }
}
